# Sanitization rules — personal info to strip
# Format: type|pattern|replacement
# This file is NOT published to the repo. The sanitize script reads it locally.

# Home directories
path|/home/user|/home/user
path|/home/user|/home/user
path|/Users/user|/Users/user
path|~/agent-workspace|~/agent-workspace
path|~/agent-workspace|~/agent-workspace
path|~/agent-workspace|~/agent-workspace

# Workspace folder names (catches after home dir replacement)
path|/agent-workspace/|/agent-workspace/
path|/agent-workspace/|/agent-workspace/
path|/agent-workspace/|/agent-workspace/
path|.openclaw/|.openclaw/

# Emails
email|user@example.com|user@example.com
email|user@example.com|user@example.com
email|user@example.com|user@example.com
email|agent@example.com|agent@example.com
email|agent@example.com|agent@example.com
email|dev@example.com|dev@example.com
email|user@example.com|user@example.com
email|admin@example.com|admin@example.com

# SSH users
ssh|henrymascot|user
ssh|jamify|user
ssh|agentsadmin|user
ssh|ubuntu|user

# Hostnames
host|<your-gateway>|<your-gateway>
host|<your-host>|<your-host>
host|<your-mac>|<your-mac>

# Known public IPs
ip|<REDACTED_IP>|<REDACTED_IP>
ip|<REDACTED_IP>|<REDACTED_IP>
ip|<REDACTED_IP>|<REDACTED_IP>

# GitHub user
github|<your-github-user>|<your-github-user>

# Secrets paths
secret|~/agent-workspace/secrets/|<YOUR_SECRET_FILE>
