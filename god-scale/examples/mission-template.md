# Mission template

Title: <small bounded change>

Acceptance command:

```bash
<command that proves success>
```

Scope:

- Allowed: <files/areas>
- Avoid: <files/areas>

Notes:

- Keep the diff small.
- Verify outside the agent loop.
- Preserve logs if verification fails.
